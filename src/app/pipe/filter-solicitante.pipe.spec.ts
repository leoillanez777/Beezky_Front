import { FilterSolicitantePipe } from './filter-solicitante.pipe';

describe('FilterSolicitantePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSolicitantePipe();
    expect(pipe).toBeTruthy();
  });
});
