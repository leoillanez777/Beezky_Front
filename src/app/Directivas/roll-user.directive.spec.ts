import { RollUserDirective } from './roll-user.directive';

describe('RollUserDirective', () => {
  it('should create an instance', () => {
    // const directive = new RollUserDirective();
    // expect(directive).toBeTruthy();
  });
});
